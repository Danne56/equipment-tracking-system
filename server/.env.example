# Database Configuration for PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=workshop_tools
DB_SSL=false

# Alternative: Use a single DATABASE_URL instead of individual settings
# DATABASE_URL=postgresql://postgres:password@localhost:5432/workshop_tools
